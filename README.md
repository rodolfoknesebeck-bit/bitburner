Things that are good: 
  starter program
    (bugs for unpurchased auxServs, maybe fix, maybe meh)

Things you should do:

figure out how to:
  sort by var attached to a name(eg. sort by number, return string(name)
  
make a better auxiliary server deleter/purchaser  

make a sorting script to choose the best target
    figure out sorting


done // make a better(ish) hacking script
        make a decent hacking script
done // one that batches the shit out of the threads
done // one that actually grows, weaken, hack



yo dafuq does Formulas.exe do?
